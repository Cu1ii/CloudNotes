<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cu1.cloudnotes.dao.CategoryMapper">
    <sql id="selectFields">
        id,user_id,category_name,create_time
    </sql>
    <sql id="insertFields">
        id,user_id,category_name,create_time
    </sql>
    <select id="selectById" resultType="Category">
        select <include refid="selectFields"> </include>
        from category
        where user_id = #{userId}
    </select>
    <insert id="insertCategory" parameterType="Category" keyProperty="id">
        insert into category (<include refid="insertFields"></include>)
        values (#{id},#{userId},#{categoryName},#{createTime})
    </insert>
    <update id="updateCategoryName">
        update category set category_name = #{CategoryName} where id = #{id}
    </update>
    <delete id="deleteByCategoryName">
        delete from category where id = #{id}
    </delete>
</mapper>
